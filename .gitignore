__pycache__/
*.pyc
.venv/
.pytest_cache/
.ruff_cache/
.DS_Store
*.egg-info/
dist/
out/
# benchmark results ARE committed (CSVs, PNGs, env.json, session logs)
# no lockfile on purpose: torch/triton are per-platform installs and the README
# says so — a committed lock (even dev-only) sends the opposite signal
uv.lock
